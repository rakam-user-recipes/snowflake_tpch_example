version: 2
models:
    #{% for i in range(1) %}
    - name: event_{{i}}
      sql: "select 1"
      meta:
          eventTimestamp: _time
          userId: _user
      columns:
          - name: device_id
            dimension:
                type: string
    #{% endfor %}
    